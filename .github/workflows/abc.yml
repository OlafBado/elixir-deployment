name: abc

on:
  workflow_run:
    workflows: [hello]
    types: [requested]
    branches: [staging, production]

jobs:
  hello:
    runs-on: ubuntu-22.04
    steps:
      - name: first
        run: echo first
